#ifndef NODO_H
#define NODO_H

struct Nodo {
    float x, y;
    Nodo(float x = 0.0, float y = 0.0); // Declaración
};

// Incluir implementación directamente aquí para asegurar la vinculación
#include "Nodo.hxx"

#endif